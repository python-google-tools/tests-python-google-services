# tests-python-google-services

Test suite for [`python-google-services`](https://github.com/python-google-tools/python-google-services).

Consumed as a git submodule at `tests/` inside the `python-google-services` repo.

## Running the tests

```bash
pytest
```
